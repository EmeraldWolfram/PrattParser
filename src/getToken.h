#ifndef getToken_H
#define getToken_H
#include "Token.h"

//FOR MOCKING PURPOSE
Token* getToken();  //get the detail of token and move the pointer to next token 
Token* peepToken(); //get the detail of token and remain the pointer to current token

#endif // getToken_H
